import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig={appId:'com.convertflow.app',appName:'ConvertFlow',webDir:'dist',server:{androidScheme:'https'}};
export default config;
